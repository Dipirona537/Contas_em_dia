package com.dipirona.contas;

import java.io.IOException;

public class principal {
    public static void main(String[] args){
        processamento.contas_valor();



        /*try {
            Runtime.getRuntime().exec("cmd /c start cmd.exe /c mkdir C:\\Contas");
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
        String conta = processamento.conta();
        System.out.println(conta);
        System.out.println(processamento.data());
        String dataed = escrita_dados.arquivoed;
        System.out.println(dataed);
        try {
            escrita_dados.criararquivo();
            escrita_dados.mover_arquivo();
        } catch (IOException e) {
            throw new RuntimeException(e);
        }*/
    }
}
